# three-bvh-csg Internal Subset

This directory contains an internal addition-only subset copied from
`external/three-bvh-csg`, which is MIT licensed.

The public project-facing API lives in `../addition.ts`. Do not import these
files directly from application code.
